<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" fill="none">
  <!-- Hintergrund-Form (Optional, für App Icons) -->
  <!-- <rect width="100" height="100" rx="20" fill="#0f172a"/> -->
  
  <!-- Das "K" Monogramm / Kalko Symbol -->
  <!-- Vertikale Säule (Law/Stability) -->
  <path d="M28 20V80" stroke="currentColor" stroke-width="12" stroke-linecap="square"/>
  
  <!-- Oberer Arm (Calculation/Graph) -->
  <path d="M72 20L28 50" stroke="currentColor" stroke-width="12" stroke-linecap="square"/>
  
  <!-- Unterer Arm (Stands fest am Boden) -->
  <path d="M48 50L72 80" stroke="currentColor" stroke-width="12" stroke-linecap="square"/>
  
  <!-- Akzent-Punkt (Precision) -->
  <circle cx="82" cy="50" r="6" fill="currentColor"/>
</svg>